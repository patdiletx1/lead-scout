# Add this to /etc/caddy/Caddyfile on the VPS
# Domain: leads.patdilet.dev → localhost:5004

leads.patdilet.dev {
    reverse_proxy localhost:5004
    basicauth {
        patdilet $2a$14$REPLACE_WITH_BCRYPT_HASH
    }
}
